# 2026-08-21

本次同步上游 `deepseek-ai/deepseek-harness` 提交范围：

`141eb6fef83422698aef7a981029e843e8161534..b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`

（即 `0.1.0-rc.8` 之后 → `0.1.1-rc.2` 发布，含图像管理策略重构、DeepSeek 视觉模型目录、确定性规范编码、read_image 尺寸报告等）

## 上游提交列表（要点）

范围共 50 个提交，以下为主要提交（按主题归并）：

| 提交 | 说明 |
| --- | --- |
| `b150a551b` | Merge pull request #2908（release/dsh-0.1.1-rc.2） |
| `aa6c361a9` | release(dsh): 0.1.1-rc.2 |
| `272ffffdd` | Merge pull request #2676（image-management-strategy，图像管理策略重构） |
| `d618bfebb` | fix(deepseek): decouple files and stream timeouts（解耦 Files 与流超时） |
| `1b389798d` | fix(llm-deepseek): fall back when Files resolution fails（Files 解析失败时回退） |
| `e30d92a03` | fix(attachment): accept opaque WebP alpha omission（接受不透明 WebP alpha 省略） |
| `577bb7141` | Merge pull request #2903（worktree-revert-2608，回退权限复制与默认修复） |
| `cbc830ade` | test(composition): remove retired image-region tool（移除已退役的 image-region 工具） |
| `2491e12fd` | refactor(attachment): normalize image storage API（规范化图像存储 API） |
| `724783b02` | refactor(image): remove region reads（移除区域读取） |
| `d29855f97` | feat(images): unify master and Files request pipeline（统一 master 与 Files 请求管道） |
| `118f24442` | fix(attachment-local): exclude metadata carriers and animation from passthrough（排除元数据载体与动画的透传） |
| `6e17c2080` | feat(tool-fs): read_image reports downscaled dimensions and coordinate scale（read_image 报告缩放尺寸与坐标比例） |
| `83a526eea` | feat(attachment-local): store a deterministic canonical image encoding（存储确定性规范图像编码） |
| `8f83853b6` | refactor(attachment): saveImage returns the canonical ref beside source facts（saveImage 返回规范引用与源事实） |
| `e637bcfb9` | Merge pull request #2726（deepseek-vision-model-catalog，DeepSeek 视觉模型目录） |
| `708a25a8a` | fix(llm-deepseek): address vision catalog review（处理视觉目录审查） |
| `528c682e0` | Merge pull request #2890（release/dsh-0.1.1-rc.1） |
| `3ec5e8f8c` | release(dsh): 0.1.1-rc.1 |
| `fa3e37982` | ci(docs): publish the documentation site from a release tag（从发布标签发布文档站点） |

## 对文档的改动

- **part3-core/ch14-llm-seam.md**（对应：DeepSeek 视觉模型目录）
  - 14.5：`llm-deepseek` 适配器描述新增 `0.1.1-rc.1` 起发布视觉模型目录，默认包含 V4 Flash / V4 Pro / V4 Flash Vision Exp（`inputModalities: ['text', 'image']`），支持多模态图像输入。
- **part3-core/ch15-capability-seams.md**（对应：图像管理策略重构）
  - 15.8：`ctx.attachments` 描述新增 `0.1.1-rc.2` 起的图像准入策略：采用**宽源信封**（32MiB / 100MP / 单边 16384px）配合**确定性规范编码**——EXIF 方向烘焙进像素、元数据剥离、长边按部署配置的规范目标缩放（默认 2048px）；PNG 调色板用于 alpha/PNG/GIF 源，JPEG 源走固定质量阶梯（85/75/60/45）直到字节目标（默认 1MiB）满足；预算内的 PNG/JPEG/WebP 逐字节透传以保证相同源去重到同一内容地址，GIF 始终重编码为 PNG 第一帧以固定"第一帧"语义。`read_image` 工具在存储发生缩放时额外在输出信封中报告原始尺寸（`sourceWidth`/`sourceHeight`）与坐标缩放因子，供程序化消费方将附图中测量的坐标映射回原始文件。

## 未影响文档的变更及原因

- **图像区域工具退役（`cbc830ade` / `724783b02`）**：`image-region` 工具已从组合测试中移除，但书中 ch15 未描述该工具的具体用法或 API，仅提及 `read_image` 作为图像读取入口，无需更新。
- **Files API 解析回退（`1b389798d` / `d618bfebb`）**：`llm-deepseek` 适配器在 Files 解析失败时的回退逻辑属于内部实现细节，ch14 描述的是适配器接缝与配置词汇，不涉及具体的 Files API 错误处理路径。
- **WebP alpha 省略接受（`e30d92a03`）**：附件接缝对不透明 WebP 的 alpha 通道省略的接受属于准入校验的内部容错，ch15 描述的是准入策略的宏观行为（宽源信封、规范编码），不涉及具体的格式校验细节。
- **权限复制与默认修复回退（`577bb7141` / `7ce85283b`）**：PR #2608 的权限复制与默认修复被完全回退，属于内部权限系统实现的调整，书中 ch15 描述的是沙箱策略的语义与事件模型，不涉及权限复制的具体实现。
- **文档站点从发布标签发布（`fa3e37982`）**：CI 工作流变化，文档站点现在从 `dsh-v*` 发布标签而非 master 分支发布，属于构建与发布流程调整，不影响书籍内容。
- **i18n 翻译链接修复（大量提交）**：上游仓库内部的双语 Agent Note 与文档链接从 `.md` 修正为 `.zh.md`，属于仓库内部文档的国际化维护，VitePress 书籍章节不涉及这些内部链接。
- **测试快照同步（`32f3c09c2` / `6816cc0b0` 等）**：权限快照、持久化轮次覆盖、图像管道覆盖等测试更新属于内部测试维护，不影响书籍描述的产品行为。
- **发布 bump 与 CI 修复（`60e44b2d7` 等）**：CI 工作流修复与发布脚本调整属于构建基础设施变化，不影响书籍内容。
- **版本标注（`0.1.1-rc.1` / `0.1.1-rc.2`）**：系发布版本推进，按"不动无关内容"原则保留书籍既有元信息未动；如需全书统改版本标注可另作一次独立修订。
