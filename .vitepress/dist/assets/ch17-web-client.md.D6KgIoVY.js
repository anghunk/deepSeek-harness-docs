import{_ as o,o as s,c as t,a2 as i}from"./chunks/framework.CCXDMzJr.js";const k=JSON.parse('{"title":"第 17 章 Web 客户端与 UI 架构","description":"","frontmatter":{},"headers":[],"relativePath":"ch17-web-client.md","filePath":"ch17-web-client.md","lastUpdated":null}'),d={name:"ch17-web-client.md"};function a(c,e,n,l,r,p){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="第-17-章-web-客户端与-ui-架构" tabindex="-1">第 17 章 Web 客户端与 UI 架构 <a class="header-anchor" href="#第-17-章-web-客户端与-ui-架构" aria-label="Permalink to &quot;第 17 章 Web 客户端与 UI 架构&quot;">​</a></h1><p>本章解析浏览器端：从页面加载、客户端 Cordis 运行时启动，到连接 Host、Slot 系统、主题 token 与类型化 RPC。核心包：<code>apps/web</code>、<code>packages/client/*</code>、<code>packages/host/*</code>、<code>packages/api/remotes</code>。</p><h2 id="_17-1-页面加载-apps-web-只是薄壳" tabindex="-1">17.1 页面加载：apps/web 只是薄壳 <a class="header-anchor" href="#_17-1-页面加载-apps-web-只是薄壳" aria-label="Permalink to &quot;17.1 页面加载：apps/web 只是薄壳&quot;">​</a></h2><p><code>apps/web</code> 不是独立应用：</p><ul><li><code>index.html</code> 只有 <code>&lt;div id=&quot;root&quot;&gt;</code> 与 <code>&lt;script src=&quot;/src/main.ts&quot;&gt;</code>；<code>main.ts</code> 只调用 <code>new AppWebEntry(el).run()</code>（注释：Everything lives in <code>@deepseek-ai/dsh-client-web</code>; this file only finds the mount point）；</li><li><code>vite.config.ts</code> 的 <code>rejectStandaloneServe</code> 插件在 <code>vite dev/preview</code> 直接抛错——<strong>bare Vite 无法注入 <code>window.__DSH_BOOT__</code></strong>，只有 <code>dsh web</code>（web profile）会在 HTML 里注入 boot 清单。这正是本书环境说明里&quot;不要另起炉灶跑替代服务器&quot;的原因：页面必须由 dsh web 宿主注入。</li></ul><p>Vite 构建把 shell 源码<strong>别名打进 bundle</strong>（<code>@deepseek-ai/dsh-client-web</code> → <code>packages/client/web/src/boot.tsx</code>），但<strong>插件包从不在此打包</strong>——插件 bundle 运行时经客户端模块系统（fetch）到达。<code>define</code> 把 vendored Loader 里的 Node-only 探测替换掉（<code>process.versions.node: &#39;0.0.0&#39;</code>、<code>node:module</code> → stub）——客户端 loader 浏览器化的关键。</p><h2 id="_17-2-启动协议-window-dsh-boot" tabindex="-1">17.2 启动协议：window.<strong>DSH_BOOT</strong> <a class="header-anchor" href="#_17-2-启动协议-window-dsh-boot" aria-label="Permalink to &quot;17.2 启动协议：window.__DSH_BOOT__&quot;">​</a></h2><p>宿主把&quot;客户端插件入口图&quot;注入页面首屏 <code>&lt;head&gt;</code>（<code>packages/client/modules/src/index.ts</code> 的 <code>injectBootManifest</code>）：<code>window.__DSH_BOOT__ = JSON</code> 作为第一个 script；<strong><code>&lt;</code> 转义为 <code>\\u003c</code></strong>——插件可控字符串不能逃出 script 元素。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> WebBootEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // 入口名 == 包名</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  url</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         // &#39;/plugins/&lt;id&gt;/client.js?rev=&lt;rev&gt;&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  rev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         // bundle 内容 hash（缓存一致性锚）</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  inject</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 依赖边（信息性）</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  immediately</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 阶段一预取标记</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>清单缺失/畸形直接 loud throw——没有合法清单就什么都引导不起来。</p><h2 id="_17-3-运行时启动-两阶段引导" tabindex="-1">17.3 运行时启动：两阶段引导 <a class="header-anchor" href="#_17-3-运行时启动-两阶段引导" aria-label="Permalink to &quot;17.3 运行时启动：两阶段引导&quot;">​</a></h2><p><code>boot.tsx</code> 的 <code>AppWebEntry.run()</code>：</p><ol><li><strong>模块面（module face）</strong>：解析 <code>__DSH_BOOT__</code> 建 <code>ClientModuleSystem</code>（lazy-CJS 模块表，Node ESM loader 的浏览器对应物），注册 shell 自有模块（<code>app-shell</code> 等），预取所有 <code>immediately</code> 行；</li><li><strong>插件面（plugin face）</strong>：<code>new Context()</code> → <code>ctx.plugin(Loader)</code>，<strong>先注入 <code>loader.internal = modules</code></strong>（模块系统挂到 Loader 上，否则 bundle import 在浏览器必炸）→ 并行 <code>loader.create({name})</code> 装载全部插件行 + <code>app-shell</code> → <code>await loader.await()</code>；</li><li><strong>启动审计</strong>：sweep 每个 entry——无 fiber = import 失败；pending = 等某个服务没等到（cordis inject 等待无超时，这个 sweep 是 fail-loud 补偿）；</li><li>全部 active → AppRoot 一次性从 loading 页切到真实 UI。</li></ol><p>设计亮点：<strong>shell 自足</strong>——loading 页在插件全部失败时也必须能工作（fail-loud 呈现不依赖它报告失败的那个系统）。</p><h2 id="_17-4-客户端插件形态" tabindex="-1">17.4 客户端插件形态 <a class="header-anchor" href="#_17-4-客户端插件形态" aria-label="Permalink to &quot;17.4 客户端插件形态&quot;">​</a></h2><p><code>package.json</code> 的 <code>dsh.client</code> 字段声明客户端插件：</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;dsh&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;client&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;inject&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@deepseek-ai/dsh-client-runtime&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;platform&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;web&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span></code></pre></div><ul><li><code>platform</code> 必须 <code>&#39;web&#39;</code>；<code>inject</code> 是依赖边；<code>immediately</code> 标记阶段一预取（<code>runtime</code> 就声明了 <code>immediately: true</code>）；</li><li>Host 半 <code>parseDshClient</code> 要求 <code>exports[&quot;./client&quot;]</code> 指向构建出的 bundle；</li><li><strong>增量扫描</strong>：<code>ClientModuleRegistry</code> 订阅 <code>internal/plugin</code>，fiber 构造/销毁标记 entry dirty，微任务批量处理——稳态一个坏包只 warning 不拖垮别人；</li><li><strong>bundle 路由</strong>：<code>GET /plugins/&lt;id&gt;/client.js?rev=&lt;rev&gt;</code>，<code>no-cache</code>（缓存一致性靠 rev query 而非 HTTP 缓存）。</li></ul><h3 id="客户端模块系统" tabindex="-1">客户端模块系统 <a class="header-anchor" href="#客户端模块系统" aria-label="Permalink to &quot;客户端模块系统&quot;">​</a></h3><p>执行插件 bundle 只<strong>注册 factory</strong>（<code>window.__ModuleLoader__.load({ id, factory })</code>），bundle body 副作用（含 CSS 注入）在 factory 闭包内、materialization 时才跑。<code>require</code> 走注册表查找，<strong>无 load 分支</strong>（跨插件 value import 是构建错误）；<code>seed.ts</code> 用 <code>satisfies</code> 把 <code>PLATFORM_MODULES</code>（react/cordis/ui-slots/web-react/ui-primitives/ui-attachment/schema-form）钉死——漏一个静态 import 编译即失败。</p><h2 id="_17-5-连接-host-双向异质通道" tabindex="-1">17.5 连接 Host：双向异质通道 <a class="header-anchor" href="#_17-5-连接-host-双向异质通道" aria-label="Permalink to &quot;17.5 连接 Host：双向异质通道&quot;">​</a></h2><p>连接<strong>不是</strong>单 WebSocket 流，而是双向异质：</p><ul><li><strong>上行（浏览器→Host）= HTTP POST RPC</strong>：<code>fetch POST /api/&lt;endpoint&gt;</code>，body <code>{ type: &#39;client-request&#39;, rpcId, method, payload }</code>；</li><li><strong>下行（Host→浏览器）= 两个只读 WebSocket</strong>：<code>/api/events.mux</code>（多路复用流）与 <code>/api/events.host</code>（host 流）。<strong>客户端在 WS 上发消息是协议违规</strong>——服务端直接 <code>close(1008, &#39;downlink only&#39;)</code>；</li><li><strong>连接循环</strong>：<code>ConnectionController</code> 每代实例私有，并行 pump 两条流，严格握手（unary describe 证明单发可达 + onOpen 证明物理流建立），失败指数退避（500ms 起 ×2，上限 10s）；</li><li><strong>信任模型</strong>：<code>isTrustedApiRequest</code> + <code>trustedHosts</code> 是 <strong>DNS-rebinding fence 而非认证</strong>；<code>PRIVILEGED_METHODS</code> 把 settings/credentials/pickDirectory/llm.discoverModels 等钉死为 loopback-only。</li></ul><h2 id="_17-6-类型化-rpc-remote-与-gateway" tabindex="-1">17.6 类型化 RPC：@Remote 与 Gateway <a class="header-anchor" href="#_17-6-类型化-rpc-remote-与-gateway" aria-label="Permalink to &quot;17.6 类型化 RPC：@Remote 与 Gateway&quot;">​</a></h2><p>三层包：<code>typert/protocol</code>（声明+修饰器，零运行时反射副作用）、<code>typert/generator</code>（TS 项目分析 → <code>InvocationDescriptor</code> 生成物）、<code>api/remotes</code>（装配）。</p><ul><li><code>@Remote</code> 标记 Host 公开实例方法；<code>@RemoteScope(key)</code> 按 merge 声明的 scoped Context kind 选接收者；<code>TypertRemoteService</code> 把 <code>super(ctx, serviceKey)</code> 绑成默认 namespace；</li><li>例证（<code>packages/host/plugin-inventory</code>）：<code>class PluginInventoryGateway extends TypertRemoteService</code>，<code>@Remote(&#39;list&#39;) list()</code> 返回插件清单快照——Host 侧零缓存 Remote-only 服务，每次直接读 Loader；</li><li><code>InvocationDescriptor</code> 是两侧共享的运行时形式：service/namespace/method/invocation（direct|context）/有序 parameters（codec：strict Zod schema 或 src-json）；参数可以是 <code>lookup</code>（Host 对象 ↔ wire id，需注册 resolver）或 <code>json</code>；<code>cancellation</code> 把 <code>AbortSignal</code> 参数标记为保留注入点（不进 wire args）；</li><li>Host Gateway <code>ctx.typertGateway</code> 在 <code>/api</code> 分派到 <code>&lt;namespace&gt;/&lt;method&gt;</code>；Client 侧 <code>ctx.remote.&lt;namespace&gt;.&lt;method&gt;</code> 调用、<code>ctx.remote.$on</code> 订阅转发事件（事件 allowlist 在 <code>api/remotes/src/remote-events.ts</code>）。</li></ul><h2 id="_17-7-slot-系统" tabindex="-1">17.7 Slot 系统 <a class="header-anchor" href="#_17-7-slot-系统" aria-label="Permalink to &quot;17.7 Slot 系统&quot;">​</a></h2><p>三层解耦：纯核心 <code>ui-slots</code>（零运行时依赖）→ Service 层 <code>ctx.slots</code>（<code>runtime/src/client/slots.ts</code>）→ 渲染器 <code>web-react/src/scoped-slots.tsx</code>。</p><h3 id="注册协议" tabindex="-1">注册协议 <a class="header-anchor" href="#注册协议" aria-label="Permalink to &quot;注册协议&quot;">​</a></h3><ul><li><code>ctx.slots.register(options, component)</code>：options 声明 <code>children</code>（否则报 &quot;slot not declared&quot;）、kind（<code>single | list | keyed | chain</code>）、scope（<code>root | session-maybe | session</code>）；</li><li><strong>声明即认领</strong>：一个 slot 只允许一个父入口 declare；重复 declare loud throw；</li><li><strong>shadowing</strong>：single 整槽一格、keyed 按 key 一格、list 按 id 一格；每格取最低 priority 的存活 entry（tie 保持注册序）；同格同 priority 二次注册 throws；chain 不 shadow（election 消费所有 entry）；</li><li><strong>失败隔离</strong>：single/keyed/list 的 entry 崩溃经 &quot;abdicate&quot; 退位让位下一个存活者（one-shot）；chain 崩溃不退位（select 时再找替代）；</li><li><strong>生命周期</strong>：disposer 移除贡献并递归 collapse 声明的子 slot；<code>slots.inject</code> 等待 slot 声明生命周期（声明已存在 → 同步跑回调，否则声明提交后跑，collapse 时 dispose）；</li><li><strong>inject 面</strong>：<code>inject: (...args) =&gt; Record&lt;string, unknown&gt;</code> 为注册者业务面；业务数据走 apply 闭包 ctx，不存在 binding 对象参数。</li></ul><h3 id="作用域标准-props" tabindex="-1">作用域标准 props <a class="header-anchor" href="#作用域标准-props" aria-label="Permalink to &quot;作用域标准 props&quot;">​</a></h3><p>框架注入的标准 props 由多个包声明合并合成：</p><ul><li><code>SessionStandardProps</code>（strict session scope）：<code>useSession</code>、<code>sessionId</code>、<code>useProjection</code>；</li><li><code>SessionMaybeStandardProps</code>（session-maybe）：可空变体；</li><li><code>GlobalStandardProps</code>（所有 slot）：<code>useSessions</code>、<code>useWorkspaces</code>。</li></ul><p>每个 <code>use&lt;Name&gt;</code> 钩子由 <code>bindSnapshotSelector</code>（<code>use-sync-external-store/with-selector</code>）构造——<strong>这是客户端栈里唯一的 hook 构造点</strong>：引擎与 host 只流动 bare observable 源，绑定发生在 React 侧。会话子树在 <code>SessionProvider</code> 里按 <code>key={sessionId}</code> remount。</p><h3 id="主题-token-系统" tabindex="-1">主题 token 系统 <a class="header-anchor" href="#主题-token-系统" aria-label="Permalink to &quot;主题 token 系统&quot;">​</a></h3><ul><li><code>--dsw-*</code> token；override 层要求 <strong><code>{light, dark}</code> 成对</strong>（单值在另一个色板下不可读）；</li><li><code>ThemeRuntime</code>：<code>getTheme()</code> 返回不可变快照；<code>setTheme()</code> 是唯一偏好写入口；<code>register()</code> 注册第三主题；<code>overrideTokens(source, tokens)</code> 是 token 级 shadowing（后层胜出，同一 source 再调替换整层并置顶，返回精确层 disposer）；</li><li><strong>首屏防闪烁</strong>：<code>injectBootTheme</code> 在 <code>&lt;body&gt;</code> 后插内联脚本（只解析 <code>system</code> 与 matchMedia，写 <code>colorScheme</code> 与 <code>data-ds-dark-theme</code>）——插件装载前的空隙期也有正确主题；</li><li>呈现层职责分离：ui-theme <strong>从不碰 DOM</strong>，ui-layout 的 ThemePresenter 消费快照落到 DOM；</li><li><code>exportInspectTokens()</code> 导出 inspect 目录——这是动态 Cordis <code>Theme.listTokens</code> inspect provider 的数据源。</li></ul><h2 id="_17-8-会话快照-事件流折叠" tabindex="-1">17.8 会话快照：事件流折叠 <a class="header-anchor" href="#_17-8-会话快照-事件流折叠" aria-label="Permalink to &quot;17.8 会话快照：事件流折叠&quot;">​</a></h2><p><code>SessionRuntime</code> 用 mintScope 模式为每会话建作用域（agent id == session id）；<code>list</code> / <code>currentProvideInfo</code> 两条 HostObservable 经 <code>bindSnapshotSelector</code> 绑成 <code>use&lt;Name&gt;</code> 钩子；事件流（<code>session/event</code> 下行）经 <strong><code>ConversationNodeAssembler</code></strong> 折叠成 <code>ConversationSnapshot</code> 提供给 UI——UI 永远渲染&quot;从日志折叠出的快照&quot;，而不是直接操作活对象（数据最小化原则）。</p><h2 id="_17-9-host-侧服务" tabindex="-1">17.9 Host 侧服务 <a class="header-anchor" href="#_17-9-host-侧服务" aria-label="Permalink to &quot;17.9 Host 侧服务&quot;">​</a></h2><ul><li><code>packages/host/webserver</code>：<code>ctx.webServer</code>——HTTP 服务、<code>tapIndex</code>（注入 boot manifest 与首屏主题脚本的 transform 钩子）；</li><li><code>frontend-static</code>：托管构建出的 web 资源；</li><li><code>plugin-inventory</code>：<code>@Remote(&#39;list&#39;)</code> 插件清单服务（上面例证）。</li></ul><h2 id="_17-10-web-profile-组成" tabindex="-1">17.10 web profile 组成 <a class="header-anchor" href="#_17-10-web-profile-组成" aria-label="Permalink to &quot;17.10 web profile 组成&quot;">​</a></h2><p><code>packages/bundle/web-app</code> 把 base bundle + webserver + client modules + UI 组件包等行组合成 web profile。第 8 章的启动链路在 Host 侧完成装载后，Host 启动 webServer，浏览器访问 3080 → 页面加载 → boot 清单 → 客户端运行时 → 连接 Host——一条完整的链路。</p><h2 id="_17-11-设计亮点小结" tabindex="-1">17.11 设计亮点小结 <a class="header-anchor" href="#_17-11-设计亮点小结" aria-label="Permalink to &quot;17.11 设计亮点小结&quot;">​</a></h2><ol><li>两阶段引导（模块面 → 插件面）与 shell 自足；</li><li>双向异质连接（HTTP 上行 + 只读 WS 下行）与 DNS-rebinding fence 信任模型；</li><li>客户端插件&quot;注册 factory、materialize 才执行副作用&quot;；</li><li>Slot 的声明即认领、shadowing 选举、abdicate 失败隔离；</li><li>主题 token 覆盖成对校验与首屏防闪烁；</li><li>@Remote 生成式契约，两侧共享 InvocationDescriptor；</li><li>会话快照折叠：UI 只见日志派生快照。</li></ol><blockquote><p><strong>文档提醒</strong>：<code>docs/subsystems/web.md</code> 讲的是 <code>ctx.web</code>（Web 搜索/抓取工具），不是 Web 客户端——读官方文档时注意区分；<code>ctx.clientModules</code>（Host 侧）与 <code>ctx.modules</code>（浏览器侧）也易混淆。</p></blockquote><p>下一部分进入全书的应用篇：插件开发技巧。</p>`,46)])])}const g=o(d,[["render",a]]);export{k as __pageData,g as default};
